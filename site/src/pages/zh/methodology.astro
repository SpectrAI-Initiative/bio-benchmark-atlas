---
import Layout from '../../components/Layout.astro';
---
<Layout title="中文方法学" canonicalPath="/zh/methodology/" lang="zh-CN"><header class="page-head narrow"><p class="eyebrow">方法学导览</p><h1>把 benchmark、评测设置和结果拆开。</h1><p class="lede">这样才能回答“谁在哪篇工作中，用什么设置，测了 benchmark 的哪一部分”。</p></header><article class="narrow section prose"><h2>四类实体</h2><ol><li><strong>Benchmark</strong>：任务定义、版本、taxonomy、题数、许可和可运行状态。</li><li><strong>Work</strong>：创建论文、system card、正式技术报告或官方发布页。</li><li><strong>EvaluationRun</strong>：一套统一的 scope、prompt、工具、budget、grader 和指标。</li><li><strong>Model</strong>：来源中出现的精确模型名；相似名称不会自动合并。</li></ol><h2>Scope 规则</h2><p><code>full</code> 必须有实际题数，而且与对应版本总数一致；<code>subset</code> 必须写清 subset、过滤条件和 n；正式 track 使用 <code>track</code>；来源没写清楚就用 <code>unknown</code>，绝不默认全量。</p><h2>缺失值</h2><p>没报告的内容统一是 <code>null + not_reported</code>，不能用 0、空字符串或推断值代替。</p><h2>结果可比性</h2><p>只在显式 <code>comparability_group</code> 内比较模型。换了工具、subset、prompt、budget、grader 或 reasoning setting，必须拆成新的 run。</p><h2>证据</h2><p>关键 setting 和公开结果需要 work、URL 以及页码/section/figure/table locator。第三方题目、附件和论文全文不镜像。</p></article></Layout>
